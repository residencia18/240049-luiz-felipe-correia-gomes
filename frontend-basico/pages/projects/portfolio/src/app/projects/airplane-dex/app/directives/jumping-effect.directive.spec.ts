import { JumpingEffectDirective } from './jumping-effect.directive';

describe('JumpingEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new JumpingEffectDirective();
    expect(directive).toBeTruthy();
  });
});
