import { RotatingBackgroundColorDirective } from './rotating-background-color.directive';

describe('RotatingBackgroundColorDirective', () => {
  it('should create an instance', () => {
    const directive = new RotatingBackgroundColorDirective();
    expect(directive).toBeTruthy();
  });
});
